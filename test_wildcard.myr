enum Shape {
    Circle { radius: i64 },
    Rect   { w: i64, h: i64 },
}

function main() {
    let s = Shape.Rect { w = 3, h = 7 }
    match s {
        Shape.Circle { radius } => { print(radius) }
        _ => { print(99) }
    }
}
