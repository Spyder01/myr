// Mirrors calls.myr but exercises the module-qualified call path (mathmod.add).
import "mathmod" as m

function main() {
    let i = 0
    let sum = 0
    for i < 100000 {
        sum = m.add(sum, i)
        i = i + 1
    }
    print(sum)
}
